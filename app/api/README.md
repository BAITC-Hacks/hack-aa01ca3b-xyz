# HTTP API

This directory is reserved for an optional FastAPI layer described in the early architecture sketch. The current hackathon prototype runs with Streamlit (`app/main.py`) and calls `app.services` directly. No REST or WebSocket endpoints are implemented, and FastAPI is not a dependency. Keep API work out of the MVP unless the team explicitly takes it on.
